package com.example.digital.automation.stepdefinitions;

//import org.monte.media.Format;
//import org.monte.screenrecorder.ScreenRecorder;

import java.awt.*;
import java.io.File;
import java.io.IOException;

public class SpecializedScreenRecorder /*extends ScreenRecorder*/ {
//    private String name;
//
//    // Constructor matching the correct ScreenRecorder signature
//    public SpecializedScreenRecorder(GraphicsConfiguration cfg, Rectangle captureSize,
//                                     Format fileFormat, Format screenFormat,
//                                     Format mouseFormat, Format audioFormat,
//                                     File movieFolder, String name) throws IOException, AWTException {
//        super(cfg, captureSize, fileFormat, screenFormat, mouseFormat, audioFormat, movieFolder);
//        this.name = name;
//    }
//
//    @Override
//    protected File createMovieFile(Format fileFormat) throws IOException {
//        if (!movieFolder.exists()) {
//            movieFolder.mkdirs();
//        }
//        // Explicitly set the file extension as .avi
//        return new File(movieFolder, name + ".avi");
//    }
}
